#include <stdio.h>
#include "main.h"
/**
 * mul - Multiplies multiple integers
 * @a: first varable to be multiplied
 * @b: second variable to be multiplied
 * Return: the multiplied value
 */
int mul(int a, int b)
{
	return (a * b);
}
