#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers and returns the result
 * @num_1: the first number to be added
 * @num_2: the second number to be added
 * Return: Always 0 (success)
 */
int add(int num_1, int num_2)
{
	return (num_1 + num_2);
}
